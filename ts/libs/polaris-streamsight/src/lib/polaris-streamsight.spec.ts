import { polarisStreamsight } from './polaris-streamsight';

describe('polarisStreamsight', () => {
    it('should work', () => {
        expect(polarisStreamsight()).toEqual('polaris-streamsight');
    });
});
